/**
 * @dexterai/vault — root entry. Re-exports the most-used types + the
 * counterfactual helper. Other subpaths (instructions, messages, reader,
 * precompile, signers, constants) are imported explicitly.
 */

export * from './types.js';
export * from './counterfactual.js';
